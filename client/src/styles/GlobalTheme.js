import { createTheme } from "@mui/material";

export const GlobalTheme = createTheme({
  palette: {
    primary: {
      main: "#112D4E",
    },
  },
  typography: {
    button: {
      textTransform: "none",
    },
  },
});
